# FazAI - Arquivo de Configuração de Ambiente
# Renomeie este arquivo para .env e ajuste as configurações conforme necessário

# Configurações do Servidor
PORT=3000                      # Porta em que o daemon será executado

# Configurações de API da OpenAI
OPENAI_API_KEY=sua_chave_aqui  # Chave de API da OpenAI para consultas de IA
OPENAI_MODEL=gpt-4             # Modelo da OpenAI a ser utilizado

# Configurações de Fallback
FALLBACK_MODEL=gpt-3.5-turbo   # Modelo de fallback caso o principal falhe
ENABLE_FALLBACK=true           # Habilitar fallback para modelo secundário

# Configurações de Log
LOG_LEVEL=info                 # Nível de log (debug, info, warn, error)
MAX_LOG_SIZE=10485760          # Tamanho máximo do arquivo de log (10MB)
MAX_LOG_FILES=5                # Número máximo de arquivos de log para rotação

# Configurações de Email (para notificações)
SMTP_HOST=smtp.example.com     # Servidor SMTP
SMTP_PORT=587                  # Porta SMTP
SMTP_USER=user@example.com     # Usuário SMTP
SMTP_PASS=senha_smtp           # Senha SMTP
NOTIFICATION_EMAIL=admin@example.com  # Email para receber notificações

# Configurações de Segurança (para implementação futura)
# ENABLE_AUTH=false            # Habilitar autenticação
# AUTH_TOKEN=token_secreto     # Token de autenticação
# ALLOWED_IPS=127.0.0.1,::1    # IPs permitidos

# Configurações de Cloudflare (para integração com API)
# CLOUDFLARE_API_TOKEN=seu_token_aqui  # Token de API da Cloudflare
# CLOUDFLARE_ZONE_ID=seu_zone_id       # ID da zona da Cloudflare
